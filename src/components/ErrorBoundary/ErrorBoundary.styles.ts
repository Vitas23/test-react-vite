import styled from '@emotion/styled';

export const ErrorInfoContainer = styled.div`
  text-align: center;
  padding-top: 4rem;
  color: #4a4a4a;
`;

export const DebugLine = styled.div`
  font-family: monospace;
  color: #666;
  font-size: 0.85rem;
  font-weight: 300;
`;
